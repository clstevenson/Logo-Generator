// TODO import Shapes class
// TODO test each class for render() method that returns a string for the corresponding SVG file with the given shape color; see below
/**
 * Example of a test that should pass:

const shape = new Triangle();
shape.setColor("blue");
expect(shape.render()).toEqual('<polygon points="150, 18 244, 182 56, 182" fill="blue" />');

**/
